{
  "name": "react-composition-structure",
  "version": "1.0.0",
  "private": true,
  "description": "React/React Native file-system composition patterns. AGENTS.md is generated from rules/ via the build script.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-agents.mjs"
  }
}
